export interface checkbox_list {
    accepts_cedit_card: boolean,
    accepts_cash: boolean,
    ravkav_services: boolean,
    sells_rvakav_reader: boolean,
    manned: boolean,
    reload_reservation: boolean
}